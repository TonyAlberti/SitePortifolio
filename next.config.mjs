/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",

  basePath: "/SitePortifolio",

  assetPrefix: "",
  trailingSlash: true,
  images: { unoptimized: true },
};

export default nextConfig;
